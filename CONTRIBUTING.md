# Contributing

日本語・英語のIssue/PRを歓迎します。大きな仕様変更は先にIssueで相談してください。

1. リポジトリをfork/cloneし、Node.js 22.13以上で `npm ci`。
2. ブランチを作成して修正。ユーザーの `.local-data/` をテストに使わないでください。
3. `npm run typecheck`、`npm test`、`npm run build` を実行。
4. UI変更は、個人情報を含まない検証用デッキでブラウザ確認。
5. PRに問題・変更後の動作・確認手順・制限を記載。

保存形式の変更は既存デッキを読み込めることを確認し、バックアップ復旧と履歴を壊さないでください。
AI変更はサーバーでも入力検証し、対象範囲と版番号を確認してください。
プレゼン中の描画は一時的なデータであり、編集内容に保存しないでください。

テストは `tests/*.test.ts` をesbuildで一時領域にコンパイルして実行します。
生成物、データベース、個人画像、会話、ログイン情報をcommitしないでください。
貢献コードはプロジェクトと同じMITライセンスで提供してください。第三者素材は出典とライセンスを明記してください。

元の開発用チェックアウトには旧Sites関連コードが残っています。
公開用ソースはローカル実行に必要なファイルだけに絞られており、Sitesの設定や認証は不要です。

公開版の番号・タグ・配布手順は [RELEASING.md](docs/RELEASING.md) に従います。
