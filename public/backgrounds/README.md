# Optional backgrounds

The public distribution does not include the author's personal backgrounds.
The editor works without them. For private use, add PNG images named `bg_1.png`
through `bg_6.png` here and rebuild. Only files that exist appear in the picker.
These names are ignored by Git and excluded from the release exporter.
Do not commit personal images. Public artwork needs an explicit license entry.
