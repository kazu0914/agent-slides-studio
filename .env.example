# This file is documentation only; the app does not automatically load .env.
# Export variables in your shell before npm start.
FRAME_PORT=9182
# Optional: absolute path to the Codex CLI (otherwise auto-detected).
# FRAME_CODEX_BIN=/path/to/codex
# Optional: absolute path to Chrome/Chromium (otherwise auto-detected).
# FRAME_CHROME_BIN=/path/to/chrome
